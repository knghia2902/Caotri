---
phase: 06-admin-order-fulfillment-dashboard-analytics
plan: 06-03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/app/actions/order.ts
  - src/components/admin/order-detail-view.tsx
  - src/app/admin/(dashboard)/orders/[id]/page.tsx
  - src/components/admin/sidebar.tsx
  - src/components/admin/header.tsx
  - scripts/test-order-admin.ts
autonomous: true
requirements:
  - ORDER-03
  - ORDER-04
must_haves:
  truths:
    - Quản trị viên/nhân viên có thể xem toàn bộ thông tin chi tiết đơn hàng tại /admin/orders/[id].
    - Có bộ nút 1-Click: Nút Gọi điện (tel:), Nút mở chat Zalo trực tiếp với số điện thoại khách hàng (zalo.me/), và Nút In phiếu giao hàng.
    - Nhân viên có thể lưu ghi chú nội bộ (Staff Notes) độc lập để ghi nhận mã vận đơn hoặc lịch sử tư vấn.
    - Chế độ in (@media print) chỉ in phiếu giao hàng tối giản với đầy đủ thông tin gửi/nhận, ẩn hoàn toàn sidebar và header quản trị.
    - Kịch bản kiểm thử scripts/test-order-admin.ts chạy thành công 100% kiểm chứng các quy trình nghiệp vụ đơn hàng.
  artifacts:
    - src/components/admin/order-detail-view.tsx
    - src/app/admin/(dashboard)/orders/[id]/page.tsx
    - scripts/test-order-admin.ts
  key_links:
    - src/app/admin/(dashboard)/orders/[id]/page.tsx kết nối Prisma Order và OrderDetailView.
    - scripts/test-order-admin.ts kiểm thử toàn diện CSDL và Server Actions.
---

<objective>
Xây dựng trang Chi tiết Đơn hàng (/admin/orders/[id]) phong cách Clean Tech Minimalist: Xem đầy đủ danh sách linh kiện đặt mua, tóm tắt thanh toán, thông tin người nhận, ghi chú của khách; Bộ nút thao tác 1-Click (Gọi điện thoại, Mở chat Zalo trực tiếp với khách, In phiếu giao hàng dán thùng); Ô lưu Ghi chú nội bộ cho nhân viên; Thiết lập giao diện In phiếu giao hàng chuyên nghiệp qua CSS print; và Xây dựng kịch bản kiểm thử tự động toàn diện đơn hàng trong CSDL.

Purpose: Trang bị cho đội ngũ bán hàng và kho vận đầy đủ công cụ chăm sóc khách, in bill giao hàng và theo dõi tiến độ đơn hàng.
Output: Server Action updateOrderNotesAction, component OrderDetailView, trang /admin/orders/[id], CSS print layout, và script test test-order-admin.ts.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-CONTEXT.md
@.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-RESEARCH.md
@.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-UI-SPEC.md
@src/app/actions/order.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng Server Action updateOrderNotesAction và Component OrderDetailView</name>
  <files>src/app/actions/order.ts, src/components/admin/order-detail-view.tsx</files>
  <action>
    1. Cập nhật `src/app/actions/order.ts`:
       - Thêm `updateOrderNotesSchema`:
         ```ts
         const updateOrderNotesSchema = z.object({
           orderId: z.string().min(1, "Thiếu mã đơn hàng"),
           adminNotes: z.string().max(2000, "Ghi chú tối đa 2000 ký tự"),
         });
         ```
       - Thêm hàm `updateOrderNotesAction(input)`:
         + Kiểm tra `requireRole(["ADMIN", "STAFF"])`.
         + Validate input với Zod.
         + Cập nhật trường `adminNotes` vào bảng `Order` trong CSDL Prisma.
         + Gọi `revalidatePath(\`/admin/orders/\${orderId}\`)`.
         + Trả về `{ success: true, message: "Đã lưu ghi chú nội bộ thành công" }`.
    2. Tạo `src/components/admin/order-detail-view.tsx`:
       - Nhận props `order` (bao gồm `items` và thông tin sản phẩm).
       - Bố cục 2 cột trên Desktop (per RESEARCH.md §4):
         + Cột trái (65%):
           * Card Danh sách linh kiện: Bảng chi tiết từng món (ảnh đại diện, tên linh kiện, đơn giá, số lượng, thành tiền).
           * Tóm tắt thanh toán: Tạm tính, Phí ship (Miễn phí), Tổng cộng to bản đậm nét.
           * Card Thông tin người nhận: Họ tên, Số điện thoại, Địa chỉ giao hàng chi tiết, Ghi chú ban đầu của khách (nếu có).
         + Cột phải (35%):
           * Card Trạng thái & Quy trình: Hiển thị trạng thái hiện tại (`OrderStatusBadge`), bộ chọn đổi trạng thái nhanh (kèm `CancelOrderDialog` nếu chọn hủy).
           * Card Ghi chú nội bộ nhân viên (Staff Notes): Textarea nhập ghi chú (mã vận đơn GHTK/GHN, lịch sử gọi xác nhận), nút "Lưu ghi chú nội bộ" gọi `updateOrderNotesAction` kèm Sonner toast.
         + Khối nút thao tác nhanh (Quick Actions Bar):
           * Nút **"Gọi điện"**: link `tel:${order.customerPhone}` (icon Phone).
           * Nút **"Mở chat Zalo"**: link `https://zalo.me/${order.customerPhone}` (target="_blank", icon MessageSquare/Zalo).
           * Nút **"In phiếu giao hàng"**: gọi `window.print()` (icon Printer).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Server Action updateOrderNotesAction và component OrderDetailView hoàn tất với đầy đủ chức năng và nút 1-Click.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng Trang /admin/orders/[id] và Tối ưu Bản In Phiếu Giao Hàng</name>
  <files>src/app/admin/(dashboard)/orders/[id]/page.tsx, src/components/admin/sidebar.tsx, src/components/admin/header.tsx</files>
  <action>
    1. Cập nhật `src/components/admin/sidebar.tsx` và `src/components/admin/header.tsx`:
       - Thêm class `print:hidden` vào thẻ ngoài cùng của `AdminSidebar` và `AdminHeader` để khi in ấn sẽ tự động ẩn thanh điều hướng và menu quản trị.
    2. Tạo `src/app/admin/(dashboard)/orders/[id]/page.tsx`:
       - Nhận dynamic params `params: Promise<{ id: string }>`.
       - Truy vấn Prisma tìm `Order` theo `id`, include `items: { include: { product: true } }`.
       - Nếu không tìm thấy: gọi `notFound()`.
       - Render `OrderDetailView`.
       - Tích hợp khung In Phiếu Giao Hàng (chỉ hiển thị khi `@media print`):
         + Tiêu đề: "CAOTRI GAMING GEAR - PHIẾU GIAO HÀNG & THU HỘ (COD)".
         + Mã đơn hàng to bản rõ nét `#DH-XXXXXX` kèm ngày giờ đặt.
         + Thông tin Người gửi (Shop CaoTrí Gear, Hotline) & Thông tin Người nhận (Tên, SĐT, Địa chỉ chi tiết, Ghi chú giao hàng).
         + Bảng danh sách sản phẩm và tổng tiền thu hộ rõ ràng.
         + Chữ ký "Người nhận hàng" và "Người giao hàng".
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Trang /admin/orders/[id] hiển thị chi tiết đơn hàng chuẩn xác, bản in phiếu giao hàng print:hidden được cấu hình tối ưu.</done>
</task>

<task type="auto">
  <name>Task 3: Viết Kịch bản Kiểm thử Tự động Quản trị Đơn hàng scripts/test-order-admin.ts</name>
  <files>scripts/test-order-admin.ts</files>
  <action>
    - Tạo `scripts/test-order-admin.ts` kiểm thử toàn bộ 7 kịch bản nghiệp vụ:
      1. Kiểm tra validation Zod (`updateOrderStatusSchema`, `updateOrderNotesSchema`): từ chối trạng thái không hợp lệ, từ chối ghi chú quá dài.
      2. Kiểm tra RBAC: gọi Server Action với role không hợp lệ hoặc không có quyền bị từ chối.
      3. Kiểm thử vòng đời đơn hàng: Tạo đơn hàng thử nghiệm ở trạng thái `PENDING` -> chuyển sang `CONTACTED` -> `SHIPPING` -> `COMPLETED`, kiểm tra CSDL cập nhật chính xác.
      4. Kiểm thử Hủy đơn an toàn: chuyển đơn sang `CANCELLED` kèm lý do, kiểm tra `status = "CANCELLED"` và nhật ký hủy được ghi nhận vào `adminNotes`.
      5. Kiểm thử Ghi chú nội bộ độc lập: lưu `adminNotes` và xác nhận `customerNotes` của khách hàng không bị ảnh hưởng.
      6. Kiểm thử tính toán doanh thu: kiểm tra truy vấn aggregate sum doanh thu thực tế (COMPLETED) và doanh thu đang xử lý.
      7. Kiểm thử hàm `computeDailyRevenue`: kiểm tra sinh đúng 7 điểm ngày liên tiếp không bị đứt đoạn.
      8. Dọn dẹp dữ liệu đơn hàng thử nghiệm sau khi hoàn tất.
  </action>
  <verify>
    <automated>npx tsx scripts/test-order-admin.ts</automated>
  </verify>
  <done>Kịch bản scripts/test-order-admin.ts thực thi đạt 100% tất cả các bài kiểm tra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> updateOrderNotesAction | Dữ liệu ghi chú nội bộ từ người dùng |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | src/app/actions/order.ts | medium | mitigate | Giới hạn độ dài adminNotes tối đa 2000 ký tự qua Zod, trim khoảng trắng |
| T-06-06 | Information Disclosure | src/app/admin/(dashboard)/orders/[id]/page.tsx | medium | mitigate | adminNotes chỉ hiển thị trong trang quản trị cho ADMIN/STAFF, không lộ ra Storefront |
</threat_model>

<verification>
- `npx tsx scripts/test-order-admin.ts`: Toàn bộ 7 bài kiểm thử chạy thành công.
- `npm run build`: Biên dịch toàn dự án thành công không lỗi, các routes `/admin/orders/[id]` sẵn sàng.
</verification>

<success_criteria>
Hoàn thành xuất sắc trang Chi tiết đơn hàng, bộ nút 1-Click Zalo/Gọi điện, bản in phiếu giao hàng và kịch bản kiểm thử đạt 100% theo chuẩn ORDER-03 và ORDER-04.
</success_criteria>

<output>
Create `.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-03-SUMMARY.md` when done
</output>
