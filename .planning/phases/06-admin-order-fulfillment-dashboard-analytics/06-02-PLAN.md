---
phase: 06-admin-order-fulfillment-dashboard-analytics
plan: 06-02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/actions/order.ts
  - src/components/admin/order-status-badge.tsx
  - src/components/admin/cancel-order-dialog.tsx
  - src/components/admin/order-table.tsx
  - src/app/admin/(dashboard)/orders/page.tsx
autonomous: true
requirements:
  - ORDER-02
  - ORDER-04
must_haves:
  truths:
    - Quản trị viên/nhân viên có thể xem danh sách tất cả đơn hàng tại /admin/orders với phân trang 10 đơn/trang.
    - Lọc đơn hàng mượt mà qua dãy Tabs trạng thái (Tất cả, Chờ xử lý, Đã liên hệ, Đang giao, Hoàn thành, Đã hủy) hiển thị số đếm thời gian thực.
    - Tìm kiếm tức thì theo mã đơn (#DH-XXXXXX), số điện thoại hoặc tên khách hàng.
    - Cho phép đổi trạng thái trực tiếp bằng dropdown selector ngay trên bảng đơn hàng; khi chọn Hủy (CANCELLED) sẽ hiển thị hộp thoại xác nhận kèm lý do.
  artifacts:
    - src/components/admin/order-status-badge.tsx
    - src/components/admin/cancel-order-dialog.tsx
    - src/components/admin/order-table.tsx
    - src/app/admin/(dashboard)/orders/page.tsx
  key_links:
    - src/app/admin/(dashboard)/orders/page.tsx kết nối URL searchParams và Prisma filters.
    - src/components/admin/order-table.tsx gọi updateOrderStatusAction trong src/app/actions/order.ts.
---

<objective>
Xây dựng trang Quản lý Danh sách Đơn hàng (/admin/orders) hoàn chỉnh: Bảng dữ liệu Clean Tech Minimalist, dãy Tabs trạng thái có số lượng đếm realtime, thanh tìm kiếm đa năng (mã đơn, SĐT, tên khách), phân trang 10 đơn/trang, tính năng đổi trạng thái nhanh trực tiếp trên từng dòng và hộp thoại xác nhận hủy đơn hàng an toàn.

Purpose: Tối ưu hóa tốc độ xử lý đơn hàng hàng ngày của nhân viên bán hàng và quản trị viên shop.
Output: Server Action updateOrderStatusAction, components OrderStatusBadge, CancelOrderDialog, OrderTable, và route /admin/orders.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-CONTEXT.md
@.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-RESEARCH.md
@.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-UI-SPEC.md
@src/app/actions/order.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xây dựng Server Action updateOrderStatusAction và Component OrderStatusBadge</name>
  <files>src/app/actions/order.ts, src/components/admin/order-status-badge.tsx</files>
  <action>
    1. Cập nhật `src/app/actions/order.ts`:
       - Định nghĩa Zod schema `updateOrderStatusSchema`:
         ```ts
         const updateOrderStatusSchema = z.object({
           orderId: z.string().min(1, "Thiếu mã đơn hàng"),
           status: z.enum(["PENDING", "CONTACTED", "SHIPPING", "COMPLETED", "CANCELLED"]),
           cancelReason: z.string().max(500).optional(),
         });
         ```
       - Thêm hàm `updateOrderStatusAction(input)`:
         + Kiểm tra phân quyền `await requireRole(["ADMIN", "STAFF"])`.
         + Xác thực đầu vào với Zod.
         + Kiểm tra tồn tại của đơn hàng.
         + Nếu trạng thái là `CANCELLED` và có `cancelReason`: ghi nhật ký hủy vào `adminNotes` (ví dụ: `[HỦY ĐƠN lúc HH:mm DD/MM bởi User]: Lý do...`).
         + Cập nhật CSDL với `prisma.order.update`.
         + Gọi `revalidatePath("/admin/orders")`, `revalidatePath("/admin")`, và `revalidatePath(\`/admin/orders/\${orderId}\`)`.
         + Trả về `{ success: true, message: "..." }`.
    2. Tạo `src/components/admin/order-status-badge.tsx`:
       - Nhận prop `status: string`.
       - Render badge màu sắc chuẩn Clean Tech Minimalist:
         + `PENDING`: Nền vàng nhạt `bg-amber-50 text-amber-700 border-amber-200`, nhãn "Chờ xử lý".
         + `CONTACTED`: Nền xanh dương `bg-blue-50 text-blue-700 border-blue-200`, nhãn "Đã liên hệ".
         + `SHIPPING`: Nền tím `bg-indigo-50 text-indigo-700 border-indigo-200`, nhãn "Đang giao".
         + `COMPLETED`: Nền xanh lá `bg-emerald-50 text-emerald-700 border-emerald-200`, nhãn "Hoàn thành".
         + `CANCELLED`: Nền xám `bg-zinc-100 text-zinc-600 border-zinc-200`, nhãn "Đã hủy".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Server Action updateOrderStatusAction và component OrderStatusBadge hoàn tất, kiểm tra type an toàn.</done>
</task>

<task type="auto">
  <name>Task 2: Xây dựng CancelOrderDialog và Bảng Đơn Hàng OrderTable</name>
  <files>src/components/admin/cancel-order-dialog.tsx, src/components/admin/order-table.tsx</files>
  <action>
    1. Tạo `src/components/admin/cancel-order-dialog.tsx`:
       - Hộp thoại Modal xác nhận hủy đơn hàng an toàn (per D-05).
       - Hiển thị cảnh báo hủy đơn hàng `#DH-XXXXXX`.
       - Trường nhập lý do hủy đơn (Textarea) tùy chọn (gợi ý nhanh các lý do: "Khách đổi ý", "Không liên lạc được", "Trùng đơn", "Sai thông tin").
       - Nút Hủy bỏ (Secondary) và Nút Xác nhận Hủy Đơn (Destructive màu đỏ `#D94A4A`).
    2. Tạo `src/components/admin/order-table.tsx`:
       - Props nhận danh sách `orders`, `totalOrders`, `currentPage`, `statusCounts`, `currentStatus`, `searchQuery`.
       - Dãy Tabs trạng thái ngang Clean Tech (`Tất cả`, `Chờ xử lý`, `Đã liên hệ`, `Đang giao`, `Hoàn thành`, `Đã hủy`) kèm badge đếm số lượng đơn thời gian thực (per D-06).
       - Ô tìm kiếm có debounce cập nhật URL query parameter `?q=...`.
       - Bảng danh sách hiển thị các cột: Mã đơn (#DH-XXXXXX click mở chi tiết), Khách hàng (Tên + địa chỉ tóm tắt), Số điện thoại (kèm nút gọi nhanh `tel:` và nút chat Zalo), Tổng tiền (VNĐ đậm), Trạng thái (Dropdown đổi trạng thái nhanh trực tiếp trên dòng), Ngày đặt, và Thao tác.
       - Khi đổi trạng thái trên dòng: Nếu chọn `CANCELLED` thì mở `CancelOrderDialog`; các trạng thái khác gọi ngay `updateOrderStatusAction` với optimistic UI và Sonner Toast.
       - Phân trang chuẩn bảng quản trị: Nút Trang trước / Trang sau, hiển thị "Trang X / Y (Tổng Z đơn hàng)".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>OrderTable và CancelOrderDialog hoàn thành, hỗ trợ lọc tab, tìm kiếm, phân trang và đổi trạng thái nhanh.</done>
</task>

<task type="auto">
  <name>Task 3: Xây dựng Trang /admin/orders Kết nối Dữ liệu Prisma</name>
  <files>src/app/admin/(dashboard)/orders/page.tsx</files>
  <action>
    - Tạo `src/app/admin/(dashboard)/orders/page.tsx`:
      - Nhận dynamic searchParams: `searchParams: Promise<{ status?: string; q?: string; page?: string }>`.
      - Phân tích điều kiện lọc:
        + `status`: Nếu có giá trị hợp lệ (PENDING/CONTACTED/SHIPPING/COMPLETED/CANCELLED) thì lọc theo status, ngược lại lấy tất cả.
        + `q`: Nếu có từ khóa thì tìm kiếm `OR` trên `orderNumber`, `customerPhone`, hoặc `customerName` (contains filter).
      - Tính toán phân trang: `take = 10`, `skip = (page - 1) * 10`.
      - Chạy truy vấn đồng thời qua `Promise.all`:
        1. `orders`: Danh sách đơn hàng theo filter, phân trang và sắp xếp `createdAt: "desc"`, include `items`.
        2. `totalOrders`: Đếm tổng số đơn theo filter hiện tại.
        3. `statusCounts`: Đếm số lượng đơn theo từng trạng thái bằng `prisma.order.groupBy` để hiển thị trên từng Tab.
      - Render component `OrderTable` truyền đầy đủ dữ liệu.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Trang /admin/orders nạp dữ liệu CSDL chính xác theo searchParams và render bảng quản lý đơn mượt mà.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> updateOrderStatusAction | Dữ liệu đầu vào từ form bảng quản trị qua Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Elevation of Privilege | src/app/actions/order.ts | high | mitigate | requireRole(["ADMIN", "STAFF"]) ngăn chặn người dùng vãng lai hoặc unauthenticated gọi action |
| T-06-04 | Tampering | src/app/actions/order.ts | medium | mitigate | Zod schema kiểm tra chặt chẽ giá trị enum của status và giới hạn độ dài lý do hủy |
</threat_model>

<verification>
- `npx tsc --noEmit`: Đạt typecheck 100%.
- `npm run build`: Xác nhận route `/admin/orders` được tạo và biên dịch thành công.
</verification>

<success_criteria>
Admin và Staff có thể quản lý, lọc theo tabs, tìm kiếm, phân trang và cập nhật trạng thái đơn hàng nhanh chóng tại /admin/orders theo yêu cầu ORDER-02 và ORDER-04.
</success_criteria>

<output>
Create `.planning/phases/06-admin-order-fulfillment-dashboard-analytics/06-02-SUMMARY.md` when done
</output>
