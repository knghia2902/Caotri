---
phase: 01-project-scaffolding-theme-database-setup
plan: 01-03
title: Seed Data Script & Demo Gaming Catalog Generation
wave: 3
depends_on:
  - 01-02
requirements:
  - FOUND-03
files_modified:
  - prisma/seed.ts
  - package.json
  - src/lib/seed-data.ts
autonomous: true
---

# Plan 01-03: Seed Data Script & Demo Gaming Catalog Generation

## Objective
Xây dựng kịch bản Seed Data phong phú cho 6 danh mục linh kiện gaming gear, nạp sẵn 15-20 sản phẩm mẫu thực tế có thông số kỹ thuật và hình ảnh sắc nét, cùng tài khoản quản trị `ADMIN` và `STAFF` có mật khẩu băm bảo mật bcrypt, giúp phân hệ Storefront và Admin sẵn sàng dữ liệu ngay từ đầu.

## Context
- Đọc `.planning/phases/01-project-scaffolding-theme-database-setup/01-CONTEXT.md` (Quyết định D-03, D-04, D-05).
- Các sản phẩm mẫu bao gồm các thương hiệu hàng đầu: Logitech G, Razer, Keychron, Akko, HyperX, SteelSeries, Asus ROG,...

## Tasks

<task type="auth-deps">
  <name>Cài đặt bcryptjs và kiểu TypeScript cho mật khẩu tài khoản</name>
  <read_first>
    - package.json
  </read_first>
  <action>
    Cài đặt `bcryptjs` và `@types/bcryptjs` (devDependencies) cùng `tsx` (hoặc `ts-node`) để chạy script seed TypeScript trực tiếp bằng lệnh `npx prisma db seed`. Cập nhật `package.json` với cấu hình `"prisma": { "seed": "npx tsx prisma/seed.ts" }`.
  </action>
  <acceptance_criteria>
    - `package.json` chứa `bcryptjs`, `@types/bcryptjs`, `tsx`.
    - Phần cấu hình `prisma.seed` trỏ vào `prisma/seed.ts`.
  </acceptance_criteria>
  <verify>
    Chạy `npx tsx --version` hoặc kiểm tra package.json.
  </verify>
</task>

<task type="seed-data">
  <name>Xây dựng bộ dữ liệu mẫu chi tiết (6 danh mục, 15-20 sản phẩm gear)</name>
  <read_first>
    - .planning/phases/01-project-scaffolding-theme-database-setup/01-CONTEXT.md
    - prisma/schema.prisma
  </read_first>
  <action>
    Tạo `src/lib/seed-data.ts` chứa dữ liệu mẫu chuẩn hóa:
    - 6 danh mục: Chuột Gaming (`chuot-gaming`), Bàn phím cơ (`ban-phim-co`), Tai nghe & Audio (`tai-nghe-gaming`), Lót chuột & Pad (`lot-chuot-pad`), Màn hình & Arm (`man-hinh-arm`), Phụ kiện & Switch (`phu-kien-switch`).
    - 15-20 sản phẩm thực tế:
      + Chuột: Logitech G Pro X Superlight 2, Razer DeathAdder V3 Pro, Pulsar X2V2.
      + Bàn phím: Keychron Q1 Pro Wireless, Akko 5075B Plus, MonsGeek M1 V3.
      + Tai nghe: HyperX Cloud II Wireless, SteelSeries Arctis Nova 7, Razer BlackShark V2 Pro.
      + Pad chuột: Artisan Ninja FX Zero, SteelSeries QcK Heavy, Pulsar Superglide.
      + Màn hình & Arm: Asus ROG Swift OLED PG27AQDM, Arm Human Motion T6 Pro.
      + Phụ kiện: Switch Gateron Oil King, Dầu bôi trơn Krytox GPL 205g0, Keycap PBT Cherry.
    - Mỗi sản phẩm gồm: tên, slug, giá gốc, giá sale, mô tả, thư viện ảnh (Unsplash/Imgur gear URLs chất lượng cao), specs (thông số kỹ thuật JSON), inStock = true, isFeatured, isNew.
    - Dữ liệu Banner trang chủ: 2-3 banner khuyến mãi & giới thiệu phong cách gaming.
    - Dữ liệu SiteSetting: Hotline (`0988.123.456`), Zalo OA link, Fanpage Facebook link, địa chỉ shop.
  </action>
  <acceptance_criteria>
    - `src/lib/seed-data.ts` định nghĩa rõ ràng cấu trúc dữ liệu cho categories, products, banners, siteSettings, users.
    - Dữ liệu có ít nhất 15 sản phẩm trải rộng trên 6 danh mục.
  </acceptance_criteria>
  <verify>
    Kiểm tra tính hợp lệ về kiểu TypeScript của `src/lib/seed-data.ts`.
  </verify>
</task>

<task type="seed-exec">
  <name>Viết kịch bản Seed Script và thực thi nạp dữ liệu vào Database</name>
  <read_first>
    - src/lib/seed-data.ts
    - prisma/schema.prisma
    - src/lib/prisma.ts
  </read_first>
  <action>
    Viết `prisma/seed.ts`:
    - Xóa dữ liệu cũ an toàn (nếu có) theo thứ tự khóa ngoại.
    - Tạo tài khoản Admin (`admin@caotri.vn` / mật khẩu `admin123@`) với role `ADMIN`.
    - Tạo tài khoản Staff (`staff@caotri.vn` / mật khẩu `staff123@`) với role `STAFF`.
    - Nạp danh mục, sản phẩm, banner và site settings từ `src/lib/seed-data.ts`.
    - Thực thi lệnh `npx prisma db seed` và in báo cáo tóm tắt số lượng bản ghi đã tạo.
  </action>
  <acceptance_criteria>
    - Lệnh `npx prisma db seed` chạy thành công không báo lỗi.
    - Số lượng Category nạp vào DB = 6.
    - Số lượng Product nạp vào DB >= 15.
    - Số lượng User = 2 (1 ADMIN, 1 STAFF).
    - Mật khẩu được mã hóa an toàn qua bcrypt.
  </acceptance_criteria>
  <verify>
    Chạy lệnh `npx prisma db seed` trong terminal và xác nhận output thông báo nạp thành công các bản ghi.
  </verify>
</task>

## Verification Plan
1. Chạy `npx prisma db seed` thành công.
2. Viết một script nhỏ hoặc test query kiểm tra số lượng bản ghi trong database:
   - `User.count()` == 2
   - `Category.count()` == 6
   - `Product.count()` >= 15
   - `Banner.count()` >= 2
3. Đăng nhập thử bằng logic verify bcrypt để chắc chắn mật khẩu hoạt động tốt cho Phase 2.

## Must Haves
### Truths
- [x] D-03: Kịch bản Seed Data nạp đầy đủ 6 danh mục và tối thiểu 15 sản phẩm gaming gear thực tế.
- [x] D-04: Mỗi sản phẩm có đầy đủ hình ảnh chất lượng cao, thông số kỹ thuật (specs) và giá bán.
- [x] D-05: Tài khoản `admin@caotri.vn` và `staff@caotri.vn` được khởi tạo với mật khẩu bcrypt sẵn sàng cho phân quyền Phase 2.
- [x] Banner và Cấu hình liên hệ (Hotline/Zalo/FB) đã được nạp sẵn.

### Artifacts this phase produces
- `prisma/seed.ts` (Database seed runner script)
- `src/lib/seed-data.ts` (Mock data fixtures for gaming gear catalog)
